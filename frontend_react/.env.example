# React frontend env example
# Set the API base URL for the backend. Do not include a trailing slash.
# If not set, the app uses same-origin requests (which will 404 unless you proxy).
#
# Typical local FastAPI backend port is 8000:
REACT_APP_API_BASE_URL=http://localhost:8000
